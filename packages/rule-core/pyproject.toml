[project]
name = "zwmp-rule"
version = "0.1.0"
description = "Core rule parsing and projection models for ZWMP"
requires-python = ">=3.11"
dependencies = [
  "pydantic>=2.7",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]

